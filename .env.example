# Optional. AI keys are configured in the browser at /setup.
# Environment variables are only a fallback if the UI has not been set up.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4.1-mini

# Recommended so Crossref can contact you if usage is high.
CROSSREF_MAILTO=you@example.com
